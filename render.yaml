services:
  - type: web
    name: checkin-bot
    env: docker
    plan: free
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: BOT_MODE
        value: webhook
      - key: LOG_LEVEL
        value: INFO
      - key: ENABLE_PERFORMANCE_MONITOR
        value: "true"
